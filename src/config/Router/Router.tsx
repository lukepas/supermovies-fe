import { BrowserRouter, Routes } from "react-router-dom";

export const Router = () => {
  return (
    <BrowserRouter basename="/">
      <Routes></Routes>
    </BrowserRouter>
  );
};

export default Router;
